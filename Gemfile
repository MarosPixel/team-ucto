source 'http://rubygems.org'

gem 'rails', '3.1.1'

gem 'jquery-rails'
gem 'haml-rails'
gem 'devise'
gem 'cancan'
gem 'simple_form'

group :assets do
  gem 'sass-rails',   '~> 3.1.4'
  gem 'coffee-rails', '~> 3.1.1'
  gem 'uglifier', '>= 1.0.3' # gem 'closure-compiler'
end

group :production do
  gem 'pg' # potrebne pre heroku
end

group :development do
  gem 'sqlite3'
end

group :test do
  gem 'sqlite3'
  gem 'turn', :require => false
end

