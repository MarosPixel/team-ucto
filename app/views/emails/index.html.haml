%h1 Mails

- if @emails.blank?

  Žiadne stiahnuté e-maily.

- else

  %table
    %tr
      %th Id
      %th Imap id
      %th State
      %th Send by
      %th Received date
      %th Download date
      %th{colspan: 2}

    - @emails.each do |email|
      %tr
        %td= email.id
        %td= email.imap_id
        %td= email.state
        %td= email.send_by
        %td= email.received_at
        %td= email.created_at
        %td= link_to 'Show', email
        %td= link_to 'Delete', email, confirm: 'Are you sure?', method: :delete

%p
  = link_to 'Delete all', emails_path, confirm: 'Are you sure?', method: :delete
%p
  = link_to 'Download Mails', process_download_path
  |
  = link_to 'Decode Attachments', process_decode_path
  |
  = link_to 'Extract Transactions', process_extract_path
  |
  = link_to 'Assign Transactions', process_assign_path

%p
  = link_to 'Start Process', process_start_path
