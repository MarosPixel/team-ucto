%h1 Listing expenses

%p
  = link_to 'Add Tournament', [:new, :tournament]
  |
  = link_to 'Add Training', [:new, :training]
  |
  = link_to 'Add Other expense', [:new, :other_expense]

%table
  %tr
    %th Id
    %th Locked
    %th Type
    %th Name
    %th User
    %th

  - @expenses.each do |expense|
    %tr
      %td= expense.id
      %td= expense.locked?
      %td= expense.type
      %td= expense.name
      %td= expense.creator.username
      %td
        = link_to 'Show', expense
        - if can? :update, expense
          = link_to 'Edit', [:edit, expense]
        - if can? :destroy, expense
          = link_to 'Destroy', expense, confirm: 'Are you sure?', method: :delete
